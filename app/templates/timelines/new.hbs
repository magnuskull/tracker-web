<h3>New timeline</h3>
<form>
  {{input value=model.name}}
  <button {{action 'saveTimeline'}} type="submit" class="btn btn-default">Save</button>
  <button {{action 'cancel'}} class="btn btn-default">Cancel</button>
</form>
